# DocNet MVP — server/.env on EC2 (production)
# Copy to /home/ubuntu/DocNetMVP/server/.env — NEVER commit real values.

MONGO_URI=mongodb+srv://akakashkumar1711_db_user:Kw1x1fwdU2ajgGnX@cluster0mvpjundocnet.rwpbl3q.mongodb.net/?appName=Cluster0MVPjunDocNet
JWT_SECRET=REPLACE_WITH_openssl_rand_hex_64
JWT_EXPIRES_IN=7d
PORT=5000
NODE_ENV=production
CLIENT_URL=http://3.7.48.54

AWS_REGION=ap-south-1
AWS_S3_BUCKET=docnet-profile-photos-akash-prod
AWS_CLOUDFRONT_URL=https://d3vs01b9563iwk.cloudfront.net
# Set only if CloudFront origin path is /profiles (leave empty if origin path is blank)
AWS_CLOUDFRONT_ORIGIN_PATH=/profiles

# Elastic-IP HTTP staging only — remove after HTTPS + domain
COOKIE_SECURE=false

# Optional third-party APIs
HF_API_KEY=
GEMINI_API_KEY=

# Platform admin — create once on EC2:
# cd server && node scripts/createPlatformAdmin.js "platform@docnet.com" "YourSecurePassword"
